{
  "type": "module",
  "scripts": {
    "dev": "cd worker && npx wrangler dev",
    "deploy": "cd worker && npx wrangler deploy",
    "db:schema": "cd worker && npx wrangler d1 execute schedule-snap-db --local --file=../schema.sql",
    "db:schema:remote": "cd worker && npx wrangler d1 execute schedule-snap-db --remote --file=../schema.sql",
    "db:patch": "cd worker && npx wrangler d1 execute schedule-snap-db --local --command \"ALTER TABLE responses ADD COLUMN critical INTEGER NOT NULL DEFAULT 0;\"",
    "db:patch:remote": "cd worker && npx wrangler d1 execute schedule-snap-db --remote --command \"ALTER TABLE responses ADD COLUMN critical INTEGER NOT NULL DEFAULT 0;\"",
    "db:tables": "cd worker && npx wrangler d1 execute schedule-snap-db --local --command \"SELECT name FROM sqlite_master WHERE type='table'\""
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "workerd@1.20260820.1": true
  }
}
